<p id='$($data.ID)'>
    $(
        if (![string]::IsNullOrWhiteSpace($data.Value)) {
            [System.Net.WebUtility]::HtmlEncode($data.Value)
        }
        else {
            Use-PodeWebPartialView -Path 'controls/_load' -Data @{ Controls = $data.Controls }
        }
    )
</p>