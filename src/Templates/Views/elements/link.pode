$(
    $target = '_self'
    if ($data.NewTab) {
        $target = '_blank'
    }

    "<a href='$($data.Source)' id='$($data.ID)' target='$($target)' class='$($data.CssClasses)'>$($data.Value)</a>"
)