<span class="form-group">
    $(
        if ($data.NewLine) {
            "<br/>"
        }

        $icon = [string]::Empty
        if (![string]::IsNullOrWhiteSpace($data.Icon)) {
            $icon = "<span class='mdi mdi-$($data.Icon.ToLowerInvariant()) mRight02'></span>"
        }

        $target = '_self'
        if ($data.NewTab) {
            $target = '_blank'
        }

        $disabled = [string]::Empty
        $disabled_link = [string]::Empty

        if ($data.Disabled) {
            $disabled = 'disabled'
            $disabled_link = "tabindex='-1' aria-disabled='true'"
        }

        if ($data.IconOnly) {
            if ($data.IsDynamic) {
                "<button type='button' class='btn btn-icon-only pode-button $($data.CssClasses)' style='$($data.CssStyles)' id='$($data.ID)' pode-object='$($data.ObjectType)' pode-data-value='$($data.DataValue)' title='$($data.DisplayName)' data-toggle='tooltip' $($disabled)>
                    $($icon)
                </button>"
            }
            else {
                "<a id='$($data.ID)' class='btn btn-icon-only pode-link-button $($data.CssClasses) $($disabled)' style='$($data.CssStyles)' pode-object='$($data.ObjectType)' href='$($data.Url)' target='$($target)' role='button' title='$($data.DisplayName)' data-toggle='tooltip' $($disabled_link)>
                    $($icon)
                </a>"
            }
        }
        else {
            if ($data.IsDynamic) {
                "<button type='button' class='btn btn-$($data.ColourType) $($data.SizeType) pode-button $($data.CssClasses)' style='$($data.CssStyles)' id='$($data.ID)' pode-object='$($data.ObjectType)' pode-data-value='$($data.DataValue)' $($disabled)>
                    <span class='spinner-border spinner-border-sm' role='status' aria-hidden='true' style='display: none'></span>
                    $($icon)
                    $($data.DisplayName)
                </button>"
            }
            else {
                "<a id='$($data.ID)' class='btn pode-link-button btn-$($data.ColourType) $($data.SizeType) $($data.CssClasses) $($disabled)' style='$($data.CssStyles)' pode-object='$($data.ObjectType)' href='$($data.Url)' target='$($target)' role='button' $($disabled_link)>
                    $($icon)
                    $($data.DisplayName)
                </a>"
            }
        }
    )
</span>